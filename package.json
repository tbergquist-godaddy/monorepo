{
  "name": "@tbergq/web-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "yarn eslint . --report-unused-disable-directives",
    "flow": "flow --include-warnings",
    "graphql": "yarn babel-node packages/relay/scripts/getSchema.js",
    "relay": "yarn relay-compiler --src . --schema ./schema.graphql --verbose",
    "build": "next build",
    "start": "next start -p $PORT",
    "heroku-postbuild": "next build",
    "test-ci": "yarn lint && yarn flow && yarn test",
    "now-build": "yarn workspace @tbergq/tvhelper build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "private": true,
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.3.4",
    "@babel/node": "^7.2.2",
    "@babel/plugin-syntax-dynamic-import": "^7.2.0",
    "@kiwicom/babel-preset-kiwicom": "^2.6.2",
    "@kiwicom/eslint-config": "^3.1.2",
    "babel-eslint": "^10.0.1",
    "eslint": "^5.15.2",
    "flow-bin": "^0.97.0",
    "jest": "^24.7.1",
    "now": "^15.0.6",
    "react-test-renderer": "^16.8.6"
  }
}
