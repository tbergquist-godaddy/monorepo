import { atoms } from '../sprinkles.css';

export const classNames = {
  error: atoms({
    color: 'danger',
    fontSize: 'normal',
    marginTop: 'tiny',
  }),
  label: atoms({
    fontSize: 'normal',
    marginBottom: 'tiny',
  }),
};
