import { atoms } from '../sprinkles.css';

export const classNames = {
  h1: atoms({
    fontSize: 'h1',
    fontWeight: 700,
  }),
  h2: atoms({
    fontSize: 'h2',
    fontWeight: 500,
  }),
  h3: atoms({
    fontSize: 'h3',
    fontWeight: 500,
  }),
  h4: atoms({
    fontSize: 'h4',
    fontWeight: 500,
  }),
  h5: atoms({
    fontSize: 'h5',
    fontWeight: 500,
  }),
  h6: atoms({
    fontSize: 'h6',
    fontWeight: 500,
  }),
};
