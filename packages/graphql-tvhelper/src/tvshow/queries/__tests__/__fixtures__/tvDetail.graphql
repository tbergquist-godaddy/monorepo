query detail {
  tvShowDetail(id:"dHZTaG93OjY=") {
    id
    name
    status
    premiered
    rating
    image {
      original
    }
    summary
    withoutTags: summary(stripTags: false)
    cast {
      person {
        name
      }
      character {
        name
      }
    }
  }
}
