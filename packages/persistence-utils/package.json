{
  "name": "@tbergq/persistence-utils",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "yarn rimraf dist && babel src --out-dir dist",
    "lint": "eslint . --fix --report-unused-disable-directives"
  }
}
