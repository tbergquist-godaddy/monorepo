{
  "name": "@tbergq/videostream",
  "version": "0.0.1",
  "sideEffects": false,
  "main": "dist/main/Main.js",
  "dependencies": {
    "@tbergq/components": "0.0.1",
    "chromecast-api": "^0.3.1",
    "compression": "^1.7.4",
    "copy-webpack-plugin": "^5.1.1",
    "cors": "^2.8.5",
    "electron": "^8.1.1",
    "express": "^4.17.1",
    "morgan": "^1.9.1",
    "network": "^0.5.0",
    "react": "^16.13.0",
    "react-dom": "^16.13.1",
    "webpack": "^4.42.0"
  },
  "scripts": {
    "build": "yarn webpack --mode production",
    "build:main": "yarn babel src/main/ --out-dir dist/main --root-mode upward -w",
    "dev-server": "yarn webpack-dev-server",
    "dev": "yarn concurrently \"yarn build:main\" \"yarn dev-server\"",
    "dev-electron": "yarn electron ."
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "babel-loader": "^8.0.6",
    "concurrently": "^5.1.0",
    "webpack-cli": "^3.3.11",
    "webpack-dev-server": "^3.10.3"
  }
}
