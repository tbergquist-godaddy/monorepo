mutation createUserMutation {
  createUser(user: {email:"test@test.com", password:"test"}) {
    __typename
    ... on User {
      id
      email
    }
    ... on GeneralError {
      message
    }
  }
}
