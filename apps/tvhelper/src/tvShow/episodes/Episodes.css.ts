import { atoms } from '@tbergq/components';

export const classNames = {
  card: atoms({
    width: '100%',
    borderStyle: 'solid',
    borderWidth: 'normal',
    borderColor: 'gray',
  }),
  title: atoms({
    fontSize: 'large',
    fontWeight: 500,
  }),
};
